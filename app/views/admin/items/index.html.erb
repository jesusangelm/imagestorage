<%= render "header" %>
<div class="items">
  <div class="container mx-auto">
    <div class="bg-white shadow-md rounded-lg overflow-hidden">
      <!-- table header -->
      <div class="flex bg-gray-200 text-gray-700 uppercase text-sm leading-normal">
        <div class="w-1/3 py-3 px-4 font-semibold">Name</div>
        <div class="w-1/3 py-3 px-4 font-semibold">Description</div>
        <div class="w-1/3 py-3 px-4 font-semibold">Category</div>
        <div class="w-1/3 py-3 px-4 font-semibold">Image</div>
        <div class="w-1/3 py-3 px-4 font-semibold">CDN Image</div>
        <div class="w-1/3 py-3 px-4 font-semibold">URL Variants</div>
        <div class="w-1/3 py-3 px-4 font-semibold">CDN URL Variants</div>
        <div class="w-1/3 py-3 px-4 font-semibold text-right">Actions</div>
      </div>
      <!-- table rows -->
      <div class="flex flex-col">
        <% if @items.any? %>
          <!-- rows -->
          <%= render @items %>
        <% else %>
          <p>No items yet</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
